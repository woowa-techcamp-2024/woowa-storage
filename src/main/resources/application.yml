spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/drive
    username: drive
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  servlet:
    multipart:
      maxFileSize: 2000MB # 파일 하나의 최대 크기
      maxRequestSize: 2000MB  # 한 번에 최대 업로드 가능 용량
      location: /Users/woowatech04/Desktop